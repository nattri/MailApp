{
  "name": "MailApp",
  "private": true,
  "version": "0.0.0",
  "description": "Basic mail application",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.6.1"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1"
  }
}
